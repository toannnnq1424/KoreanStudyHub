<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.16</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.ksh</groupId>
	<artifactId>ksh</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>ksh</name>
	<description>Korean Study Hub</description>

	<properties>
		<java.version>17</java.version>
		<maven.compiler.release>${java.version}</maven.compiler.release>
		<aws.sdk.version>2.46.8</aws.sdk.version>
		<!-- Security/compatibility bridges released after the final Boot 3.5 BOM. -->
		<commons-lang3.version>3.20.0</commons-lang3.version>
		<!-- POI 5.5.1 is tested against this Commons Codec API level. -->
		<commons-codec.version>1.20.0</commons-codec.version>
		<!-- 2.21.5 closes the 2026 JsonView/JsonIgnoreProperties bypass advisories. -->
		<jackson-bom.version>2.21.5</jackson-bom.version>
		<logback.version>1.5.38</logback.version>
		<!-- 2.25.5 closes the 2026 Log4j API/layout serialization advisories. -->
		<log4j2.version>2.25.5</log4j2.version>
		<!-- 5.4.3 closes the 2026 HTTP/1.1 and HTTP/2 header exhaustion advisories. -->
		<httpcore5.version>5.4.3</httpcore5.version>
		<!-- 4.1.136.Final closes the 2026 Netty HTTP/SPDY denial-of-service advisories. -->
		<netty.version>4.1.136.Final</netty.version>
		<tomcat.version>10.1.57</tomcat.version>
		<cyclonedx-maven-plugin.version>2.9.2</cyclonedx-maven-plugin.version>
		<dependency-check-maven.version>12.2.2</dependency-check-maven.version>
		<jacoco.version>0.8.12</jacoco.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>software.amazon.awssdk</groupId>
				<artifactId>bom</artifactId>
				<version>${aws.sdk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<!-- Web (Spring MVC) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<!-- Thymeleaf SSR -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>
		<!-- JPA / Hibernate -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<!-- Spring Security -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<!-- WebSocket (STOMP messaging for real-time chat, KSH-8.3) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-websocket</artifactId>
		</dependency>
		<!-- Bean Validation -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<!-- Flyway migration (MySQL) -->
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-mysql</artifactId>
		</dependency>
		<!-- Spring Security OAuth2 Client (Google sign-in) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-client</artifactId>
		</dependency>
		<!-- JavaMailSender (forgot-password email) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<!-- Spring Cache abstraction + Caffeine provider (in-process cache for hot reads) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
		</dependency>
		<!-- Thymeleaf + Spring Security integration (sec:authorize, ...) -->
		<dependency>
			<groupId>org.thymeleaf.extras</groupId>
			<artifactId>thymeleaf-extras-springsecurity6</artifactId>
		</dependency>

		<!-- Apache POI: Excel parsing for student import (KSH-3.4) -->
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi-ooxml</artifactId>
			<version>5.5.1</version>
		</dependency>

		<!-- Jsoup: HTML sanitizer for Lesson rich-text body (KSH-4.0b) -->
		<dependency>
			<groupId>org.jsoup</groupId>
			<artifactId>jsoup</artifactId>
			<version>1.22.2</version>
		</dependency>

		<!-- AWS SDK v2 — S3 client used against Cloudflare R2 (Object Storage).
		     url-connection-client avoids pulling Netty into the servlet stack. -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>s3</artifactId>
		</dependency>
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>url-connection-client</artifactId>
		</dependency>

		<!-- MySQL driver (runtime) -->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!-- Lombok: bỏ getter/setter/builder thủ công -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
		<!-- DevTools: hot reload khi dev -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>

		<!-- Test (JUnit 5, Mockito, AssertJ, ...) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- STOMP-over-SockJS client assets for the messaging UI (KSH-8.3).
		     webjars-locator-core resolves version-agnostic /webjars/** paths. -->
		<dependency>
			<groupId>org.webjars</groupId>
			<artifactId>sockjs-client</artifactId>
			<version>1.5.1</version>
		</dependency>
		<dependency>
			<groupId>org.webjars</groupId>
			<artifactId>stomp-websocket</artifactId>
			<version>2.3.4</version>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.pdfbox</groupId>
			<artifactId>pdfbox</artifactId>
			<version>3.0.8</version>
		</dependency>
		<dependency>
			<groupId>org.webjars</groupId>
			<artifactId>webjars-locator-core</artifactId>
			<version>0.59</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<executions>
					<execution>
						<id>enforce-ksh-build-toolchain</id>
						<phase>validate</phase>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireJavaVersion>
									<version>[17,18)</version>
									<display>true</display>
									<message>
										KSH requires JDK 17. Align JAVA_HOME, the IntelliJ
										project SDK, Maven importer/runner and Spring Boot run
										configuration with Java 17.
									</message>
								</requireJavaVersion>
								<requireMavenVersion>
									<version>[3.9,4)</version>
									<message>
										KSH requires Maven 3.9.x. Use the repository mvnw
										wrapper so CLI and IntelliJ execute the same Maven line.
									</message>
								</requireMavenVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<!-- Coverage is measured, reported and archived; it is not a proxy for
			     correctness, so no artificial 100% build threshold is imposed. -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals><goal>prepare-agent</goal></goals>
					</execution>
					<execution>
						<id>jacoco-report</id>
						<phase>test</phase>
						<goals><goal>report</goal></goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<!-- spring-boot:run reads HTML/CSS/JS directly from
					     src/main/resources, so UI edits only need a browser
					     refresh during local development. This option affects
					     the run goal, not the packaged production JAR. -->
					<addResources>true</addResources>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>phase13h-security-audit</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.cyclonedx</groupId>
						<artifactId>cyclonedx-maven-plugin</artifactId>
						<version>${cyclonedx-maven-plugin.version}</version>
						<configuration>
							<outputFormat>json</outputFormat>
							<outputName>practice-phase13h-sbom-2026-07-28</outputName>
							<outputDirectory>${project.build.directory}/phase13h-security</outputDirectory>
							<includeCompileScope>true</includeCompileScope>
							<includeRuntimeScope>true</includeRuntimeScope>
							<includeTestScope>true</includeTestScope>
							<skipAttach>true</skipAttach>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.owasp</groupId>
						<artifactId>dependency-check-maven</artifactId>
						<version>${dependency-check-maven.version}</version>
						<configuration>
							<formats>
								<format>JSON</format>
								<format>HTML</format>
							</formats>
							<outputDirectory>${project.build.directory}/phase13h-security</outputDirectory>
							<failBuildOnCVSS>7.0</failBuildOnCVSS>
							<failOnError>true</failOnError>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
